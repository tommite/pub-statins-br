model {
  for (i in 1:length(r)) {
    r[i] ~ dbin(p[i], n[i])
    logit(p[i]) <- mu[i]
    mu[i] ~ dnorm(theta, tau)
  }
  theta ~ dnorm(0, 0.0001)
  sd ~ dunif(0,2)
  tau <- pow(sd, -2) 
  pred ~ dnorm(theta, tau)
}
